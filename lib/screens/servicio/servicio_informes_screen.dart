import 'package:flutter/material.dart';

class ServicioInformesScreen extends StatelessWidget {
  const ServicioInformesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
